import jsonwith open('mp.json') as f:    dfd = json.load(f)csvl = []rec = {}for d in dfd:    for l in d['listings']:        rec = l.copy()        rec.update(d)        for v in rec['verticals']:            rec[v] = True        del rec['verticals']        rec[rec['sellerId']] = True        del rec['sellerId']        del rec['id']        del rec['listings']        csvl.append(rec)import pandas as pddf = pd.DataFrame(csvl)